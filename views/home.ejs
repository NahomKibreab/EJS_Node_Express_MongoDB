<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <h1><%= !category ? 'All' : category%> Product list</h1>
    <% for(product of products){ %>
        <li><a href="product/<%= product.id %> "><%= product.name %></a></li>
    <% } %>  
    <a href="/product" <%= !category ? 'hidden':'' %> >Show All</a> <a href="/product/new">Add New</a>
</body>
</html>